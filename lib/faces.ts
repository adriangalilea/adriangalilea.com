// THE SITE'S SERIF, MEASURED. The quote card turns a measure in characters into a width in
// pixels through the face's average character advance, and a still cannot measure text,
// so whoever names the face says how wide it runs. Instrument Serif is condensed: left at
// the generic default the type came out a third too small for its measure, a haiku in a
// corner of the frame.
//
// Measured with fontTools over English letter frequencies (spaces included) on
// lib/fonts/InstrumentSerif-Regular.ttf. Re-measure if the face changes; never guess.
export const SERIF_CH = 0.333;
